export const DEV_VENUE_ID = 'v_7ykrc92wuw58gbrgyicr7e';
export const DEV_AUTO_SIGNUP = true; // only applies to DEV_ACCOUNT_EMAIL
export const DEV_ACCOUNT_EMAIL = 'test@example.com';

/**
 * DEV ONLY:
 * When true, App.tsx signs out on first mount so you ALWAYS see the Login screen.
 * Flip back to false when you want to retain sessions like production.
 */
export const DEV_FORCE_SIGNOUT_ON_START = true;
