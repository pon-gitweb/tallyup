// @ts-nocheck
// Stub — stockTakePhotos
export async function createStockTakePhotoDoc({ venueId, areaId, photoUrl }: any): Promise<{ id: string }> {
  console.log('[stocktake] createStockTakePhotoDoc stub', { venueId, areaId });
  return { id: 'stub-photo-doc' };
}
