defaults.url=https://sentry.io/
defaults.org=stack-mosaic-ltd
defaults.project=react-native
# SENTRY_AUTH_TOKEN must be set as an EAS secret:
#   eas secret:create --scope project --name SENTRY_AUTH_TOKEN --value <your-token>
# The token needs these scopes: project:read, project:releases, org:read
